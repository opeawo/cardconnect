# Get a key at https://platform.deepseek.com/
DEEPSEEK_API_KEY=

# Optional: override the SQLite file path. Useful for production deployments
# where you want the DB on a mounted persistent disk.
# DATABASE_PATH=/var/data/cardconnect.db
